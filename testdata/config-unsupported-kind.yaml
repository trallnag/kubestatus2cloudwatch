seconds: 5

metric:
  namespace: MyNamespace
  name: PromstackUp
  dimensions:
    - name: Cluster
      value: MyCluster

targets:
  - kind: Job
    namespace: default
    name: socat-tunnel
    mode: AtLeastOne
