seconds: 5

metric:
  namespace: MyNamespace
  name: PromstackUp
  dimensions:
    - name: Cluster
      value: MyCluster

targets:
  - kind: Deployment
    namespace: default
    name: socat-tunnel
    mode: FooBar
